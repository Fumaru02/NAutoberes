import 'package:flutter/material.dart';

class WorkShopManagerView extends StatelessWidget {
  const WorkShopManagerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}