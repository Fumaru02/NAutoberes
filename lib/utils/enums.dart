enum AlignTextType {
  left,
  center,
  right,
}

enum EndPointName {
  // auth keywords
  home,
  authorize,
  accessToken,
  refreshToken,
  applicationInfo,
  userLogout,
  subdistrict,
  village,
  dptList,
  dptAdd,
  logout,
}

enum SnackbarType { error, success, info, custom }
